import 'package:flutter/material.dart';

class SizedBoxT extends StatelessWidget {
  const SizedBoxT({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 10,
    );
  }
}
